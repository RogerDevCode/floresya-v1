name: FloresYa CI/CD Pipeline - Optimized

permissions:
  contents: read
  security-events: write
  pull-requests: write
  issues: write

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  NODE_VERSION: '20.x'
  COVERAGE_THRESHOLD: 30

jobs:
  lint-and-format:
    name: Lint & Format Check
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run ESLint
      run: npm run lint
      continue-on-error: false

    - name: Check Prettier formatting
      run: npm run format:check
      continue-on-error: true

    - name: Validate API client sync
      run: npm run validate:client:sync
      continue-on-error: true

    - name: Validate API Contract
      run: npm run validate:contract:ci
      continue-on-error: false

  test-coverage:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest
    needs: lint-and-format

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run all tests with coverage
      run: npm run test:coverage
      env:
        NODE_ENV: test
      continue-on-error: false

    - name: Check coverage thresholds
      run: |
        if [ -f coverage/coverage-summary.json ]; then
          echo "✅ Coverage summary found"
          
          LINES=$(jq -r '.total.lines.pct' coverage/coverage-summary.json)
          FUNCTIONS=$(jq -r '.total.functions.pct' coverage/coverage-summary.json)
          BRANCHES=$(jq -r '.total.branches.pct' coverage/coverage-summary.json)
          STATEMENTS=$(jq -r '.total.statements.pct' coverage/coverage-summary.json)

          echo "Coverage Report:"
          echo "Lines: $LINES%"
          echo "Functions: $FUNCTIONS%"
          echo "Branches: $BRANCHES%"
          echo "Statements: $STATEMENTS%"

          # Fail if below threshold
          THRESHOLD=${{ env.COVERAGE_THRESHOLD }}
          FAILED=0
          
          if [ $(echo "$LINES < $THRESHOLD" | awk '{print ($1 < $3)}') -eq 1 ]; then
            echo "❌ Lines coverage below ${THRESHOLD}%: $LINES%"
            FAILED=1
          fi
          if [ $(echo "$FUNCTIONS < $THRESHOLD" | awk '{print ($1 < $3)}') -eq 1 ]; then
            echo "❌ Functions coverage below ${THRESHOLD}%: $FUNCTIONS%"
            FAILED=1
          fi
          if [ $(echo "$BRANCHES < $THRESHOLD" | awk '{print ($1 < $3)}') -eq 1 ]; then
            echo "❌ Branches coverage below ${THRESHOLD}%: $BRANCHES%"
            FAILED=1
          fi
          if [ $(echo "$STATEMENTS < $THRESHOLD" | awk '{print ($1 < $3)}') -eq 1 ]; then
            echo "❌ Statements coverage below ${THRESHOLD}%: $STATEMENTS%"
            FAILED=1
          fi

          if [ $FAILED -eq 1 ]; then
            echo "❌ Coverage thresholds not met"
            exit 1
          fi

          echo "✅ Coverage check completed"
        else
          echo "⚠️ No coverage summary found"
          exit 1
        fi
      continue-on-error: false

    - name: Run Performance Gates
      run: npm run test:unit test/performance/critical-paths.test.js
      continue-on-error: false

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v4
      if: ${{ always() }}
      continue-on-error: true
      with:
        file: ./coverage/lcov.info
        fail_ci_if_error: false
        token: ${{ secrets.CODECOV_TOKEN }}

    - name: Upload coverage HTML report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-report-${{ github.run_number }}
        path: coverage/
        retention-days: 7
        if-no-files-found: ignore

  security-scan:
    name: Security Scanning
    runs-on: ubuntu-latest
    needs: lint-and-format

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run npm audit
      run: |
        # Audit production dependencies only (exclude devDependencies like clinic.js)
        npm audit --omit=dev --audit-level=moderate --json > npm-audit-results.json || true
        if [ -f npm-audit-results.json ]; then
          VULNERABILITIES=$(jq -r '.metadata.vulnerabilities.total // 0' npm-audit-results.json)
          echo "Found $VULNERABILITIES production vulnerabilities"
          
          if [ "$VULNERABILITIES" -gt 0 ]; then
            echo "⚠️ Production vulnerabilities found:"
            jq '.metadata.vulnerabilities' npm-audit-results.json
          fi
          
          # Only fail if critical/high vulnerabilities in production deps
          CRITICAL=$(jq -r '.metadata.vulnerabilities.critical // 0' npm-audit-results.json)
          HIGH=$(jq -r '.metadata.vulnerabilities.high // 0' npm-audit-results.json)
          
          if [ "$CRITICAL" -gt 0 ] || [ "$HIGH" -gt 5 ]; then
            echo "❌ Critical or too many high vulnerabilities in production dependencies"
            exit 1
          fi
          
          echo "✅ Production dependencies security check passed"
        fi

    - name: Run Snyk scan
      uses: snyk/actions/node@master
      continue-on-error: true
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

    - name: Upload security scan results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: security-scan-results-${{ github.run_number }}
        path: npm-audit-results.json
        retention-days: 7
        if-no-files-found: ignore

  build-and-validate:
    name: Build & Validate
    runs-on: ubuntu-latest
    needs: [lint-and-format, test-coverage]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build CSS
      run: npm run build:css

    - name: Validate build artifacts
      run: |
        echo "🔍 Validating build artifacts..."
        if [ ! -f "public/css/tailwind.css" ]; then
          echo "❌ CSS build failed - tailwind.css not found"
          exit 1
        fi
        echo "✅ Build artifacts validated"

    - name: Run contract validation
      run: npm run validate:full
      continue-on-error: true

    - name: Check deployment readiness
      run: |
        echo "🚀 Checking deployment readiness..."
        
        # Check package.json integrity
        npm ls --depth=0 > /dev/null 2>&1 || echo "⚠️ Package dependency warnings"

        # Check for critical files
        CRITICAL_FILES=("package.json" "api/server.js" "api/app.js" "public/index.html")
        for file in "${CRITICAL_FILES[@]}"; do
          if [ ! -f "$file" ]; then
            echo "❌ Critical file missing: $file"
            exit 1
          fi
        done

        echo "✅ Deployment readiness checks passed"

    - name: Create deployment artifact
      run: |
        echo "📦 Creating deployment artifact..."
        mkdir -p dist
        cp -r api/ dist/
        cp -r public/ dist/
        cp package.json dist/
        cp package-lock.json dist/
        echo "✅ Deployment artifact created"

    - name: Upload deployment artifact
      uses: actions/upload-artifact@v4
      with:
        name: deployment-artifact-${{ github.run_number }}
        path: dist/
        retention-days: 7

  e2e-tests:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    needs: [build-and-validate]
    timeout-minutes: 20
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Build CSS
      run: npm run build:css

    - name: Create .env file
      run: |
        echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" >> .env
        echo "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" >> .env
        echo "SUPABASE_KEY=${{ secrets.SUPABASE_KEY }}" >> .env
        echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env
        echo "SESSION_SECRET=${{ secrets.SESSION_SECRET }}" >> .env
        echo "NODE_ENV=test" >> .env
        echo "PORT=3000" >> .env
        echo "BASE_URL=http://localhost:3000" >> .env

    - name: Seed test data
      run: npm run seed:test
      env:
        NODE_ENV: test
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      continue-on-error: true

    - name: Start App
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        JWT_SECRET: ${{ secrets.JWT_SECRET }}
        SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
        NODE_ENV: test
        PORT: 3000
      run: |
        echo "🚀 Starting app on port 3000..."
        npm start &
        
        echo "Waiting for app service..."
        timeout 120 bash -c 'until curl -f http://localhost:3000/health 2>/dev/null; do sleep 5; done' || {
          echo "❌ App service failed to start"
          exit 1
        }
        echo "✅ App service is ready!"

    - name: Run Playwright tests
      run: npm run test:e2e
      env:
        CI: true
        NODE_ENV: test
        BASE_URL: http://localhost:3000

    - name: Upload Playwright report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: playwright-report-${{ github.run_number }}
        path: playwright-report/
        retention-days: 7
        if-no-files-found: ignore

  deploy-preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    needs: [build-and-validate]
    if: github.event_name == 'pull_request'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Deploy to Vercel Preview
      run: |
        echo "📝 Preview deployment would go here"
        echo "PR: ${{ github.event.pull_request.number }}"

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build-and-validate, e2e-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build CSS
      run: npm run build:css

    - name: Deploy to Production
      run: |
        echo "🚀 Production deployment would go here"
        echo "Branch: ${{ github.ref }}"

    - name: Create deployment tag
      run: |
        TAG_NAME="deploy-$(date +%Y%m%d-%H%M%S)"
        echo "Created tag: $TAG_NAME"
