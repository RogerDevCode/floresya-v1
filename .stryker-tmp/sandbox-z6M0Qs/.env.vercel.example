# Vercel Environment Variables Configuration
# Copy these to Vercel Dashboard → Settings → Environment Variables

# ⚠️ TEMPORARY: Enable mock authentication in Vercel (DEVELOPMENT ONLY)
# ⚠️ REMOVE BEFORE REAL PRODUCTION DEPLOYMENT
ALLOW_MOCK_AUTH=true

# Supabase Configuration
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Node Environment (keep as production for performance)
NODE_ENV=production

# Optional: Force real authentication (overrides mock)
# FORCE_AUTH=true
